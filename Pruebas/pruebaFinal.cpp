int main() {
    int limite = 15;
    int contador = 0;
    float factor = 1.5;
    string mensaje = "Calculando...";

    int resultado = (limite * 2) + contador;

    if (resultado > 20) {
        contador = contador + 1;
    } else {
        contador = contador - 1;
    }

    while (contador < 5) {
        contador = contador + 1;
    }

    int x = 10;
    x = 10;

    return 0;

    limite = 999; 
}