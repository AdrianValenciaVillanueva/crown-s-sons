if (x > 0) { x = x + 1; } else { x = x - 1; }
while (y < 10) { y = y + 1; }
for (i = 0; i < 10; i = i + 1) { suma = suma + i;} 

if (x > 0 { x = x + 1; }  // Paréntesis desbalanceados
while x < 10) { y = y + 1; }  // Paréntesis faltantes
for i = 0; i < 10; i = i + 1) { suma = suma + i; }  // Paréntesis faltantest