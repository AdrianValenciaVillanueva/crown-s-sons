// 1. Asignación de Constantes a Variables
int x = 5;
int y = 10;

// 2. Uso de Variables simples
int z = x + y;

// 3. Expresiones Aritméticas (con paréntesis)
int result1 = a * (b + c);

// 4. Expresiones Lógicas
bool flag = (x > y) && (z < 10);

// 5. Expresiones Anidadas y Temporales
int total = (a + b) * (c - d);
int result2 = (x + y) / (z - w);

// 6. Gestión de Errores (División por 0 y error de tipos)
int result3 = a / 0;
int totalError = a + "string";